<% content_for :title, '参加しているグループ - ' %>
<div class="w-full">
  <div class="flex justify-between items-center mb-4">
    <h1 class="font-bold text-4xl">参加しているグループ</h1>
    <%= link_to 'グループを作る', new_group_path, class: 'text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2' %>
  </div>

  <div id="groups" class="min-w-full space-y-2">
    <% if @groups.present? %>
      <%= render @groups %>
      <%= paginate @groups %>
    <% else %>
      <div class="p-4 my-4 text-blue-800 border border-blue-300 rounded-lg bg-blue-50" role="alert">
        <div class="flex items-center">
          <h3 class="text-xl font-medium">グループに参加していません！</h3>
        </div>
        <div class="mt-2 mb-4">
          <p>グループを作るか、グループへの招待を受けてください！</p>
          <p>グループを作ると、グループ内のメンバーにポッドキャストを公開できます！</p>
        </div>
        <div class="flex">
          <%= link_to 'グループを作る', new_group_path, class: 'text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2' %>
        </div>
      </div>
    <% end %>
  </div>
</div>
