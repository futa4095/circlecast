<% content_for :title, "新しい番組の作成 - " %>
<div class="mx-auto md:w-2/3 w-full">
  <div class="mb-2">
    <%= link_to @channel.group.name, @channel.group, class: 'text-xl' %>
  </div>
  <h1 class="font-bold text-3xl">新しい番組の作成</h1>

  <%= render 'form', channel: @channel %>

  <%= link_to '戻る', group_channels_path(@channel.group), class: 'text-gray-600 hover:text-white border border-gray-600 hover:bg-gray-600 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm py-3 text-center inline-block w-28' %>
</div>
