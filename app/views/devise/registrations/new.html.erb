<% content_for :title, "#{t('.sign_up')} - " %>
<div class="flex flex-col gap-4">
  <p class="font-logo text-4xl text-blue-600 text-center"><%= link_to 'CIRCLECAST', root_path %></p>
  <h1 class="font-bold text-3xl text-center"><%= t('.sign_up') %></h1>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render 'devise/shared/error_messages', resource: %>

    <div class="my-5">
      <%= f.label :name, class: 'mb-1 block font-medium text-gray-800' %>
      <%= f.text_field :name, autofocus: true, class: 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5' %>
    </div>

    <div class="my-5">
      <%= f.label :email, class: 'mb-1 block font-medium text-gray-800' %>
      <%= f.email_field :email, autocomplete: 'email', placeholder: 'email@example.com', class: 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5' %>
    </div>

    <div class="my-5">
      <%= f.label :password, class: 'mb-1 font-medium text-gray-800' %>
      <%= f.password_field :password, autocomplete: 'new-password', class: 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5' %>
      <% if @minimum_password_length %>
        <p class="mt-2 text-sm text-gray-500"><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></p>
      <% end %>
    </div>

    <div class="my-5">
      <%= f.label :password_confirmation, class: 'mb-1 block font-medium text-gray-800' %>
      <%= f.password_field :password_confirmation, autocomplete: 'new-password', class: 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5' %>
      <p class="mt-2 text-sm text-gray-500">確認のため、再度パスワードを入力してください。</p>
    </div>

    <div class="my-3">
      <%= f.submit t('.sign_up'), class: 'text-white bg-blue-600 hover:bg-blue-700 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 cursor-pointer' %>
    </div>
  <% end %>

  <%= render 'devise/shared/links' %>
</div>
